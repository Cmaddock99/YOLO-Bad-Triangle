model:
  name: yolo
  params:
    model: yolo26n.pt

data:
  source_dir: outputs/ci_demo/images

attack:
  name: none
  params: {}

defense:
  name: none
  params: {}

predict:
  conf: 0.5
  iou: 0.7
  imgsz: 640

validation:
  enabled: false
  dataset: configs/coco_subset500.yaml
  params: {}

summary:
  enabled: false

runner:
  seed: 42
  max_images: 1
  output_root: outputs/framework_runs/ci
  run_name: ci_demo

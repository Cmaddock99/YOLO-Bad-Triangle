[tool.pytest.ini_options]
addopts = "-m 'not integration'"
pythonpath = ["src"]
markers = [
    "integration: end-to-end tests that require the full environment (dataset, model weights, GPU/CPU)",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"scripts/*" = ["E402"]
"tests/*" = ["E402"]

[tool.mypy]
# Current enforced scope is limited to the paths below. Planned expansion order:
# keep src/lab/runners stable, then extend to src/lab/plugins, src/lab/config,
# src/lab/attacks, src/lab/defenses, and finally scripts/automation/auto_cycle.py.
python_version = "3.11"
ignore_missing_imports = true
disable_error_code = ["import-untyped"]
warn_unused_configs = true
pretty = true
files = [
    "src/lab/eval",
    "src/lab/reporting",
    "src/lab/runners",
]

# Core YOLO framework and experiment utilities
# Supported local runtime: Python 3.11.x.
# To install: python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# For a reproducible install, use: pip freeze > requirements-lock.txt after installing.

# Ultralytics YOLO implementation and training/inference tooling
ultralytics==8.4.23

# PyTorch backend for model training/inference
torch==2.5.1

# TorchVision datasets, transforms, and model utilities aligned with torch 2.5.1
torchvision==0.20.1

# Numerical operations and tensor/image array manipulation
numpy>=1.23.5,<2.0.0

# OpenCV bindings for image/video loading and preprocessing
opencv-python>=4.8.0,<5.0.0

# Tabular experiment logging and analysis
pandas>=1.5.0,<2.3.0

# Progress bars for training/evaluation loops
tqdm>=4.66.0,<5.0.0

# YAML config parsing for experiment settings
pyyaml>=6.0.0,<7.0.0
